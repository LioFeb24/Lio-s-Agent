# 本地 AI Agent 项目与 Hermes Agent 框架对比分析

## 1. 文档目的

本文档用于系统整理当前本地项目的逻辑框架、核心算法与调用链路，并与 `Hermes Agent` 的公开框架能力进行对比。

对比目标不是判断“谁更好”，而是回答 3 个问题：

1. 当前项目已经具备了哪些 Agent 核心能力。
2. 当前项目与 Hermes Agent 在架构层面分别偏向什么形态。
3. 如果要把当前项目继续演进到更完整的 Agent 平台，还缺哪些关键层。

Hermes Agent 参考仓库：

- https://github.com/nousresearch/hermes-agent

---

## 2. 当前项目总体定位

当前项目更适合定义为：

> 一个以本地桌面使用为核心、支持 CLI 与 GUI 双入口、具备会话记忆、自动 chat/exec 分流、任务规划执行验证闭环的单代理本地执行 Agent。

它已经不是简单聊天壳层，原因在于它具备以下 4 个关键特征：

1. 有统一运行时编排层 `AgentRuntime`。
2. 有可落地执行的 `ExecService` 状态机。
3. 有会话恢复、摘要归档、跨会话记忆拼接。
4. 有 GUI/CLI 共享同一套业务核心，而不是两套独立逻辑。

相对地，Hermes Agent 更适合定义为：

> 一个面向长期演进的通用 Agent 平台，强调跨会话记忆、技能系统、工具生态、环境适配、多平台接入与自我改进闭环。

因此，两者的差异主要不是“有没有 Agent”，而是：

- 当前项目偏“单体型本地执行 Agent”
- Hermes Agent 偏“平台型通用 Agent 框架”

---

## 3. 当前项目的真实架构分层

### 3.1 入口层

- `ai_agent.py`
  - 顶层兼容入口。
  - 实际只转发到 `core.cli.main()`。
- `GUI.py`
  - 图形界面入口。
  - 负责界面、线程、事件队列、会话列表重建、状态展示。
  - 不直接承载业务核心，主要通过 `AgentRuntime` 调用业务组件。

### 3.2 编排层

- `core/agent_runtime.py`
  - 是当前项目最核心的运行时编排器。
  - 持有并统一管理：
    - `SessionManager`
    - `MemoryManager`
    - `CommandHandler`
    - `ChatService`
    - `ExecService`
  - 对外暴露统一接口给 CLI 和 GUI：
    - 普通聊天
    - `/exec`
    - `/end`
    - `/rm`
    - `/session list`
    - `/session <id>`

### 3.3 会话层

- `core/session_manager.py`
  - 负责 session 的创建、恢复、保存、切换、归档、删除。
  - 通过 `session_state` 目录保存完整会话历史。
  - 通过“当前指针文件”记录当前选中的 session。
  - 已归档的 session 仍保留完整 `history`，支持之后再次切换恢复。

### 3.4 记忆层

- `core/memory_manager.py`
  - 负责读取 `MEMORY` 目录中的摘要记忆。
- `core/prompt_builder.py`
  - 负责把摘要记忆拼接成长期上下文。
  - 负责把当前 session 历史拼接成当前上下文。
  - 负责构造聊天 prompt、总结 prompt。

### 3.5 指令层

- `core/command_handler.py`
  - 负责识别 `/end`、`/rm`、`/exec`、`/session` 等指令。
- `core/instruction_loader.py`
  - 从 `instruction.json` 递归提取 `instruction` 字段，建立指令表。

### 3.6 对话层

- `core/chat_service.py`
  - 负责普通聊天 prompt 组装。
  - 负责附件读取、截断、注入 prompt。
  - 负责流式输出和 reasoning token 回调。

### 3.7 执行层

- `core/exec_service.py`
  - 负责任务规划、步骤执行、结果观察、步骤验证、失败修复、最终确认。
  - 这是当前项目与一般聊天程序差异最大的部分。

### 3.8 执行后端层

- `Executor`
  - 支持 `shell` / `python` / `file` / `tool` 四类 step。
- `core/sendbox/cube_sandbox.py`
  - 提供 CubeSandbox/E2B 兼容沙箱适配能力。
  - 在启用时可把执行从宿主机切到沙箱。

### 3.9 配置层

- `core/get_config.py`
  - 负责读取、补齐并写回 `config.json`。
  - 把 GUI 配置、sandbox 配置、router LLM 配置统一收口。

---

## 4. 当前项目的核心数据流

### 4.1 启动阶段

启动后，系统执行的主流程可以概括为：

1. 读取 `config.json`
2. 获取当前用户
3. 恢复当前 session 或创建新 session
4. 读取全部历史摘要记忆
5. 构建 `AgentRuntime`
6. 等待 CLI 输入或 GUI 事件

其本质是：

- `配置加载`
- `会话恢复`
- `记忆拼接`
- `统一运行时暴露`

### 4.2 普通聊天阶段

普通聊天不是直接把用户输入发给大模型，而是先走以下路径：

1. GUI/CLI 接收输入
2. `CommandHandler` 判断是否是显式指令
3. 若不是指令，进入 `AgentRuntime.chat()`
4. `ExecService.maybe_run_from_user_input()` 先判断当前输入是否应转自主执行
5. 若判断为 `chat`：
   - `ChatService` 组装记忆、历史、附件
   - 调用主模型
   - 流式回调到 GUI/CLI
6. 把用户输入与回复写回 session history

这里有一个重要设计点：

> 普通聊天入口已经具备“自动进入本地执行”的能力，所以 `/exec` 不再是唯一执行入口，而只是显式兼容入口。

### 4.3 自动 chat / exec 分流算法

当前项目没有继续沿用简单关键词匹配，而是：

1. 提取最近 10 轮对话
2. 组装成结构化对话轮次 JSON
3. 发送给 `intent_router` 模型
4. 只允许输出：
   - `{"way":"chat"}`
   - `{"way":"exec"}`
5. 当 `way=exec` 时自动进入执行闭环

该机制的意义是：

- 不只看单句关键词
- 开始利用上下文语义判断是否需要“本地执行”
- 将“是否调用执行器”本身也交给 LLM 路由器做决策

这已经具备了简化版 Agent Router 的味道。

### 4.4 `/exec` 显式执行路径

显式 `/exec` 流程如下：

1. 用户输入 `/exec <任务>`
2. `CommandHandler` 抽取任务内容
3. `AgentRuntime.execute_exec_workflow()` 调用 `ExecService.run()`
4. `ExecService` 进入状态机
5. 规划 plan 和 steps
6. 顺序执行 step
7. 每个 step 执行后做 LLM 验证
8. 失败时修复 `command` 或 `script_content` 后重试
9. 全部完成后做 final verify
10. 生成 result / verify / report 文件
11. 将最终 Markdown 报告写回聊天记录

### 4.5 `/end` 归档路径

当前项目的 `/end` 设计已经不是阻塞式单线程：

1. 前台立即切到新会话
2. 旧会话快照交给后台线程
3. 后台生成摘要 JSON
4. 写入 `MEMORY`
5. 刷新会话列表显示

这意味着 GUI 已经具备了较好的交互响应性：

- 结束会话后不必等待摘要模型完全跑完
- 用户能立刻继续聊天或切换 session

### 4.6 GUI 与运行时的耦合方式

GUI 当前的职责比较清晰：

- 管界面布局
- 管事件队列
- 管线程与状态标签
- 管 session 列表渲染
- 管聊天气泡渲染

业务逻辑仍然下沉在 `AgentRuntime` 和各 `core` 组件中，这种拆分比把逻辑直接堆在 `GUI.py` 内部更健康。

---

## 5. 当前项目的核心算法与机制

### 5.1 会话恢复算法

恢复逻辑本质上是一个“优先级回退链”：

1. 优先看当前 session 指针文件
2. 若不存在，尝试恢复旧版 active session
3. 若仍失败，扫描该用户的未归档 session
4. 还没有则新建 session

这个设计保证了：

- 正常退出可以续接
- 异常中断可以续接
- 旧结构可以迁移
- 没有可恢复数据时也能平滑回退

### 5.2 记忆生成算法

当前项目的长期记忆机制是“摘要记忆”而不是“检索记忆”。

核心流程：

1. 会话结束时把完整历史交给 summary 模型
2. 固定输出 `summary + summary_title`
3. 按文件写入 `MEMORY`
4. 下次聊天时把全部摘要拼接为 memory context

优点：

- 简单稳定
- 易于理解
- 成本可控
- 便于 session 列表展示

缺点：

- 没有精细检索
- 没有实体级记忆
- 没有用户偏好建模
- 上下文增长时会越来越粗糙

### 5.3 执行规划算法

`ExecService` 的计划生成本质上是：

1. 将执行器 schema 暴露给 LLM
2. 约束 LLM 输出标准化 JSON
3. 要求返回：
   - `task`
   - `tool_schema`
   - `plan`
   - `workflow`
   - `steps`
4. 再把 LLM 输出归一化为内部 step 结构

这套设计的重要价值在于：

- 不是让 LLM“随便写命令”
- 而是先把“可执行动作空间”结构化，再让模型规划

这已经接近一个轻量 Agent Planner。

### 5.4 执行状态机算法

当前项目最关键的算法是执行状态机：

- `PLAN`
- `EXECUTE`
- `VERIFY_STEP`
- `RETRY_STEP`
- `VERIFY_FINAL`
- `DONE`
- `FAILED`

这是一个典型的闭环自治执行器：

1. 先规划
2. 再执行
3. 再验证
4. 不通过则修复
5. 最终整体确认

它与“只执行一次 shell 命令”的脚本型方案相比，差异非常大，因为它已经具备：

- step-level verification
- 基于失败原因的修复
- 统一报告生成
- 后端切换能力

### 5.5 Step 验证算法

验证不是只依赖 return code，而是让 LLM 综合判断：

- task 目标
- step 定义
- command / script_content
- stdout
- stderr
- verify 条件

因此它比“returncode == 0 就算成功”更接近真实任务完成判定。

### 5.6 失败修复算法

当某个 step 失败时：

1. 把失败原因、执行结果和原 step 一起发给 LLM
2. 限制 LLM 只能修改：
   - `command`
   - `script_content`
3. 保留：
   - `id`
   - `title`
   - `kind`
   - `verify`
4. 重新执行

这是一种很有价值的“受约束自修复”策略。

它避免了失败后整套计划被模型随意改写。

### 5.7 附件注入算法

普通聊天支持文本和图片附件：

- 文本附件会读取内容并在超长时截断
- 图片附件会注入 base64
- 这些内容会作为 prompt 的一部分送入模型
- 同时在 session history 中只记录附件名称摘要

这说明当前项目已经不只是单纯“输入一行文字 -> 输出一行文字”，而是具备多模态输入接口的雏形。

---

## 6. Hermes Agent 的框架特征概括

根据 Hermes Agent 的公开说明，其核心特征可归纳为以下几类。

### 6.1 平台定位

Hermes Agent 的定位不是单机聊天工具，而是“能持续成长”的 Agent 平台：

- 可运行在本地、VPS、GPU 集群或 serverless 环境
- 可通过 CLI、Telegram、Discord、Slack 等多入口交互
- 可以跨会话持续积累知识与用户模型

### 6.2 核心能力

Hermes Agent 强调：

- 内建学习闭环
- 技能创建与技能改进
- 持久记忆与跨会话搜索
- 用户画像沉淀
- 调度与自动化
- 子代理并行化
- 多终端 / 多执行环境运行

### 6.3 典型框架层

从公开能力反推，Hermes Agent 更接近这样的平台层次：

1. 入口层
   - CLI
   - Messaging Gateway
2. 编排层
   - 主 Agent runtime
   - 子代理派发
3. 记忆层
   - 持久记忆
   - 历史检索
   - 用户画像
4. 技能层
   - 技能注册
   - 技能执行
   - 技能自改进
5. 工具层
   - 工具集与 toolset
   - MCP 集成
6. 环境层
   - 本地
   - Docker
   - SSH
   - Serverless/Remote backend
7. 自动化层
   - 定时调度
   - 周期任务

从这个角度看，Hermes Agent 的目标是“长期运行的通用 Agent 操作系统”，而不只是“有执行器的聊天程序”。

---

## 7. 当前项目 vs Hermes Agent 对比

## 7.1 架构定位对比

| 维度 | 当前项目 | Hermes Agent |
| --- | --- | --- |
| 总体形态 | 单体型本地执行 Agent | 平台型通用 Agent 框架 |
| 核心入口 | CLI + GUI | CLI + Messaging Gateway + 多平台 |
| 编排中心 | `AgentRuntime` | 更完整的 runtime + gateway + environment |
| 运行重点 | 本地桌面、项目内执行 | 跨平台、跨会话、长期运行 |

结论：

- 当前项目已经有明确的 Agent runtime。
- Hermes Agent 更进一步，把 runtime 扩展成了平台。

## 7.2 运行时编排对比

### 当前项目

- 单一主运行时对象统一编排业务组件。
- 普通聊天可自动切换到本地执行。
- 显式 `/exec` 与隐式自主执行共用同一个执行器。

### Hermes Agent

- 更强调多入口接入和长期运行。
- 更可能围绕任务、工具、技能、环境做更宽的编排。
- 公开能力中已经出现“子代理并行化”。

结论：

- 当前项目强在“业务流程收口清晰”。
- Hermes 强在“编排范围更宽、可扩展度更高”。

## 7.3 执行闭环对比

### 当前项目

当前项目在执行闭环上的成熟度非常高，已经具备：

- 结构化 planning
- 线性 step 执行
- step-level verification
- 失败修复
- final verification
- Markdown 报告
- 宿主机 / 沙箱切换

### Hermes Agent

Hermes Agent 公开能力更偏向：

- 通用工具调用
- 长期任务运行
- 子代理并行
- 多环境执行
- 自动化调度

结论：

- 当前项目在“本地线性自治执行器”这一点上，完成度并不低。
- Hermes 的优势不一定是单条执行链更细，而是任务编排范围更广、生命周期更长。

## 7.4 记忆系统对比

### 当前项目

- 长期记忆 = 会话摘要文件
- 短期记忆 = 当前 session history
- 启动时拼接全部历史摘要

### Hermes Agent

- 强调持久记忆
- 强调跨会话搜索
- 强调用户模型
- 具备更明显的“记忆主动沉淀”特征

结论：

- 记忆层是当前项目与 Hermes 的最大差距之一。
- 当前项目是“摘要堆叠式记忆”。
- Hermes 更接近“可检索、可建模、可持续增长的记忆系统”。

## 7.5 技能系统对比

### 当前项目

当前项目有不少“隐式技能”：

- 规划技能
- step 验证技能
- step 修复技能
- 总结归档技能
- 附件理解技能

但这些能力全部写死在服务流程里，没有独立技能资产层。

### Hermes Agent

Hermes Agent 强调：

- Skills
- 技能创建
- 技能改进
- 技能持久化

结论：

- 当前项目有技能能力，但没有技能系统。
- Hermes 把能力资产化、模块化了。

## 7.6 工具系统对比

### 当前项目

支持的执行动作主要是：

- `shell`
- `python`
- `file`
- `tool`

其中内置 tool 仍较少，主要是：

- `list_dir`
- `read_text`
- `path_exists`
- `glob`

### Hermes Agent

根据公开描述，它更强调：

- 丰富工具集
- toolset 概念
- MCP 扩展
- 多后端终端环境

结论：

- 当前项目已有工具执行底座。
- Hermes 的差异在于工具生态更完整、工具治理更体系化。

## 7.7 平台与部署对比

### 当前项目

- 面向本地 Windows 桌面使用
- 已有 GUI
- 已支持沙箱执行
- 仍主要围绕单机目录和本地项目工作区

### Hermes Agent

- 可运行于 CLI、消息平台、远端环境、容器、serverless
- 更适合跨设备、跨会话、跨平台持续交互

结论：

- 当前项目是“本地桌面 Agent 产品雏形”。
- Hermes 更像“Agent 平台基础设施”。

---

## 8. 当前项目的优势总结

即使对照 Hermes，当前项目依然有几个很明显的优势。

### 8.1 业务骨架清晰

当前项目已经完成了从“脚本堆叠”到“组件分层”的升级：

- 入口层
- 编排层
- 会话层
- 记忆层
- 聊天层
- 执行层
- 沙箱层

这使得后续继续扩展时，不需要推倒重来。

### 8.2 执行闭环成熟

在“本地任务落地执行”这件事上，当前项目已经有较成熟的算法骨架：

- 路由
- 规划
- 执行
- 观察
- 验证
- 修复
- 报告

这部分实际上已经非常像一个简化的 Agent executor。

### 8.3 GUI 与核心逻辑分离

很多同类项目会把逻辑都写在 GUI 里，导致后面无法维护。

当前项目没有走这条路，而是让 GUI 主要负责：

- 可视化
- 线程
- 事件
- 状态反馈

核心业务仍在 `core`，这是非常正确的方向。

### 8.4 会话与摘要设计可直接产品化

当前项目的 session 切换、归档、恢复、前后台分离处理都已经有产品交互意识，而不只是技术验证。

---

## 9. 当前项目相对 Hermes 的缺口

如果以 Hermes Agent 为参照系，当前项目的主要缺口如下。

### 9.1 缺少显式技能注册层

当前项目的技能还内嵌在流程 prompt 中，没有：

- 技能注册表
- 技能输入输出 schema
- 技能权限边界
- 技能版本管理
- 技能路由逻辑

### 9.2 缺少强检索型记忆系统

当前项目只有摘要型长期记忆，没有：

- 向量检索
- 语义搜索
- 用户偏好建模
- 任务轨迹检索
- 可回溯记忆索引

### 9.3 缺少多代理/并行任务模型

当前执行器仍是线性串行：

- 一次一个任务
- 一次一个 step
- 没有任务树
- 没有子代理分工
- 没有并发执行

### 9.4 缺少更完整的平台层

当前项目没有：

- API 服务层
- 网关层
- 远端执行 worker
- 任务调度中心
- 可观测性后台

### 9.5 缺少工具治理层

虽然已支持 `tool` step，但还缺：

- 统一 tool registry
- 更丰富工具元数据
- 工具权限审批
- 调用审计
- 速率限制与策略控制

---

## 10. 如果继续演进，建议的升级方向

如果目标是把当前项目逐步演进到更接近 Hermes 的框架形态，建议按下面顺序推进。

### 第一阶段：夯实当前单代理执行框架

优先补齐：

1. Tool Registry
2. 统一 Step Schema / Tool Schema 文档化
3. 执行 trace 结构化落盘
4. 更清晰的失败分类

原因：

- 这些改动不推翻现有架构
- 但能显著提升可维护性和可扩展性

### 第二阶段：升级记忆系统

建议增加：

1. 摘要记忆 + 检索记忆双层结构
2. 用户偏好记忆
3. 任务结果索引
4. 按主题或实体检索历史

原因：

- 当前项目与 Hermes 的差距，最大就在“长期记忆的可用性”

### 第三阶段：抽象技能系统

建议把现有隐式技能拆成显式技能，例如：

- `plan_task`
- `verify_step`
- `repair_step`
- `summarize_session`
- `analyze_attachment`

再为每个技能定义：

- 名称
- 输入 schema
- 输出 schema
- 调用策略

### 第四阶段：扩展平台层

可以逐步加入：

1. 本地 HTTP API
2. Web 面板或远端控制入口
3. 定时任务
4. 并行子任务执行
5. 外部消息入口

---

## 11. 最终结论

一句话总结：

> 当前项目已经具备“本地自治执行 Agent”的核心骨架，尤其在执行状态机、会话恢复、摘要归档、GUI/CLI 共用运行时方面完成度较高；但它距离 Hermes Agent 这种“可持续成长的通用 Agent 平台”，仍主要差在记忆系统、技能系统、工具治理、多代理并行与平台化接入层。

更具体地说：

- 如果目标是“做一个可用的本地桌面 Agent”，当前项目已经跨过了最难的骨架阶段。
- 如果目标是“对齐 Hermes Agent 的平台化能力”，下一步应优先补：
  - 检索型记忆
  - 显式技能系统
  - 工具注册与治理
  - 并行任务与子代理
  - 多入口与自动化平台层

因此，当前项目最准确的判断不是“落后”，而是：

> 已经完成了 Hermes 风格平台的前半段，即单代理本地执行骨架；后半段则还需要围绕记忆、技能、平台三大方向继续抽象与扩展。
